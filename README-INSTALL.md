# MENCONS Runtime — Instalación (HP-UX)
Paquete delta desde `git diff` (solo deploy/).
- `DELETE-LIST.txt` contiene archivos a eliminar (con backup).
- Copiará el resto con permisos adecuados.
## Instalación
./install.sh
